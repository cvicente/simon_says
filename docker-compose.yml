---

version: "3.3"
services:
  simon_says:
    container_name: simon_says
    build:
      context: .
    restart: always
    expose:
      - "5060"
      - "8000"
    volumes:
      - ./config.ini:/etc/simon_says.ini
      - ./asterisk_configs:/etc/asterisk
    network_mode: host
